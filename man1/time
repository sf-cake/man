名称
  time -- 记录命令执行时间

使用方法
  time [-al][-h | -p][-o file]组件名 [参数 ...]

说明
  time组件运行并记录指定组件的执行时间。当组件执行完成后，time会将信息写入
  错误流中(stderr，单位：秒)，其中包含三个部分：总耗时，执行组件进程的时间
  和系统分摊时间。

  该组件提供如下选项：

  -a	如果使用-o标识，则追加结果到输出文件末尾，否则此选项无效。

  -h	显示用户友好可读的时间格式，时间会被划分为时、分等即使单位。

  -l	以rusage(*) 结构打印结果

  -o file
	将结果输出为文件，如果目标文件已存在且未指定-a标识，则该文件被覆盖
	重写。

  -p	以POSIX.2的标准显示时间输出信息（每个时间均被显示在独立的一行中）

  一些shell会提供自己的内建time命令，功能与time组件的功能一致，详细请参考
  builtin(1)中的内容。

  如果time接收到一个SIGINFO信号(具体请参考状态参数stty(1))，time会返回当前
  已执行的时间信息。

环境
  如果使用组件名称时未含有'/'字符，则通过PATH环境变量获取该组件的位置。

退出状态
  如果目标组件被执行成功，time返回目标组件的退出状态值。如果目标组件非正常
  终端，time会在输出设备中输出警告信息至stderr。如果目标组件存在却不可执行
  则返回状态码126。如果目标组件不存在，则返回状态码127。其他time相关的状态
  执行后均会返回一个1～125之间的状态码。

参考
  builtin(1),csh(1),getrusage(2),wait(2)

标准
  time组件被要求与ISO/IEC 9945-2:1993("POSIX")标准一致。

历史
  time组件首次出现于AT&T UNIX 第三版。

注释
  1.rusage 即Resource Usage使用定义，具体可参考getrusage(2)
