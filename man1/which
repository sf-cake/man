名称
  which -- 定位程序文件的路径

使用方法
  which [-as]程序 ...

说明
  which组件返回所有可执行或调用的命令名对应的路径。

  该组件提供如下选项:

  -a	列出所有可执行的实例（否则只返回第一个查询结果）。
  -s	无输出内容，如果可执行实例存在返回0，否则返回1.

  一些shell会提供自己的内建which命令，功能与treu组件功能一致，详细请参考
  builtin(1)中的内容。

参考
  builtin(1),csh(1),find(1),locate(1),whereis(1)

历史
  which首次出现于FreeBSD2.1版本

作者
  which组件早先由Wolfram Schneider<wosch@FreeBSD.org>用Perl编写。当前版
  本的which组件则是由Daniel Papasian<dpapasia@andrew.cmu.edu>用C语言编写。
